# backend

FastAPI backend: ingestion pipeline, RAG chat API, review queue, eval harness.

See `../PHASES.md` for the module breakdown (`ingest/`, `api/`, `llm/`, `eval/`) and `../CLAUDE.md` for code rules.
